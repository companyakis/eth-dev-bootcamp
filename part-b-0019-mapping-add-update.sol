mapping(address => uint256) public balance;

balance[address(0x123)] = 10; // add

balance[address(0x123)] = 20; // update





// For mappings, we could have adding, updating, deleting, and querying

