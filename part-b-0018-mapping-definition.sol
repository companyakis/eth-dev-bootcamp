// Create a mapping that has a key of type address and a value of type uint named pool.

mapping(address => uint) pool;







// mapping(address => mapping(uint256 => uint256)) public nestedMapping;
